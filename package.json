{
  "name": "xml_slot_parser",
  "version": "1.0.5",
  "description": "Parses xml to json and filters files based on key pair values",
  "keywords": [
    "XML",
    "Filter",
    "By Key"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "clean": "rm -rf dist/*",
    "prepare": "npm run clean && npm run build",
    "prepublishOnly": "npm test"
  },
  "author": "Jory Braun",
  "license": "ISC",
  "dependencies": {
    "xml-js": "^1.6.8"
  },
  "devDependencies": {
    "@types/jest": "^23.3.2",
    "@types/node": "^10.11.0",
    "jest": "^23.6.0",
    "ts-jest": "^23.10.2",
    "typescript": "^3.0.3"
  }
}
